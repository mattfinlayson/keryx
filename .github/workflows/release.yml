name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  macos:
    name: macOS app bundle
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - name: Release build
        run: swift build -c release

      - name: Generate app icon
        run: |
          swift Scripts/make-icon.swift build/AppIcon.iconset
          iconutil -c icns build/AppIcon.iconset -o build/AppIcon.icns

      - name: Package Keryx.app
        env:
          VERSION: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          APP="Keryx.app"
          mkdir -p "$APP/Contents/MacOS" "$APP/Contents/Resources"
          cp .build/release/KeryxApp "$APP/Contents/MacOS/keryx"
          cp build/AppIcon.icns "$APP/Contents/Resources/AppIcon.icns"
          cat > "$APP/Contents/Info.plist" <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" http://www.apple.com/DTDs/PropertyList-1.0.dtd>
          <plist version="1.0">
          <dict>
            <key>CFBundleName</key><string>Keryx</string>
            <key>CFBundleDisplayName</key><string>Keryx</string>
            <key>CFBundleIdentifier</key><string>com.mattfinlayson.keryx</string>
            <key>CFBundleExecutable</key><string>keryx</string>
            <key>CFBundlePackageType</key><string>APPL</string>
            <key>CFBundleShortVersionString</key><string>${VERSION#v}</string>
            <key>CFBundleVersion</key><string>${VERSION#v}</string>
            <key>LSMinimumSystemVersion</key><string>15.0</string>
            <key>CFBundleIconFile</key><string>AppIcon</string>
            <key>CFBundleIconName</key><string>AppIcon</string>
            <key>NSHighResolutionCapable</key><true/>
            <key>LSUIElement</key><true/>
          </dict>
          </plist>
          PLIST
          codesign --force --sign - "$APP"
          zip -qr "Keryx-${VERSION}-macos.zip" "$APP"

      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ github.ref_name }}
        run: |
          gh release create "$VERSION" "Keryx-${VERSION}-macos.zip" \
            --repo "${{ github.repository }}" \
            --generate-notes \
            --title "Keryx $VERSION"